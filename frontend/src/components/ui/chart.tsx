cd /Users/askhat/projects/post-monitoring-app/frontend

mkdir -p src/components/ui

# Открой debiuro-source/chart.tsx и скопируй его содержимое
# (можно через pbcopy/pbpaste или вручную).
# Предположим, что ты его уже скопируешь в буфер.
# Тогда:
pbpaste > src/components/ui/chart.tsx